# Optional: some tools (e.g. IDEs) read `.env`; `uv run` may not pass this to `python scripts/...`
# on all versions. Scripts that import `src` load `scripts/_repo_root.py` first, so you usually
# do not need this file for those commands.
#
# If you still want PYTHONPATH for other commands:
PYTHONPATH=.
